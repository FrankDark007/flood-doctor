name = "flood-doctor-forms"
main = "form-handler.js"
compatibility_date = "2024-01-01"

# Custom domain (optional - can use workers.dev subdomain)
# routes = [
#   { pattern = "api.flood.doctor/forms/*", zone_name = "flood.doctor" }
# ]
