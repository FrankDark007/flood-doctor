
export const siteConfig = {
  SITE_URL: 'https://flood.doctor',
  // Set to true to enforce trailing slashes (e.g. /about/), false to remove them (e.g. /about)
  CANONICAL_TRAILING_SLASH: true, 
  DEBUG: true,
  DEBUG_CONTENT_ENGINE: false, // Set to true to see content audit banners on ServiceDetail pages
};